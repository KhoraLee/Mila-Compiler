program factorialRec;

function fact(n: integer): integer;
var
    m: integer;

begin
    writeln(m);
    if (n = 0) then
        fact := 1;
    else
        fact := n * fact(n - 1);
end;

var
    m: integer;

begin
    readln(m);
    writeln(fact(m));
end.